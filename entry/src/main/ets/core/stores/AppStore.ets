export interface UserState {
  isLoggedIn: boolean;
  userId: string;
  displayName: string;
}

export interface ThemeState {
  isDarkMode: boolean;
}

export function initAppStore(): void {
  // AppStorage 閸掓繂顫愰崠?(娴ｈ法鏁ゆ导鐘电埠閺傜懓绱?
}
